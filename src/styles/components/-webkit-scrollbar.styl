$style-scroll()
    &::-webkit-scrollbar
        width 1px
        height 1px

    &::-webkit-scrollbar-track
        background: $colors["light"];

    &::-webkit-scrollbar-thumb
        background: $colors["brand"];

// .style-scroll
//     $style-scroll()
